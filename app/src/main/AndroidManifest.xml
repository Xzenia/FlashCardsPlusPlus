<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.treskie.conrad.flashcardsplus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Add.AddCard" />
        <activity android:name=".Add.AddDeck" />
        <activity android:name=".Viewer.CardViewer" />
        <activity android:name=".Edit.EditCard" />
        <activity android:name=".Browser.CardBrowser"/>
        <activty android:name=".Add.AddImage" />
        <activity android:name=".Edit.RenameDeck"/>
        <activity android:name=".Add.AddImage" />

    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
</manifest>